This folder contains classes required to work with Retrofit library.
As Retrofit uses GSON we need to provide json schema classes to parse
responses. These "schemas" can be found in ./primitives folder.